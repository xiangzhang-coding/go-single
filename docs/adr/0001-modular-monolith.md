# 模块化单体架构，不拆微服务

学习项目以单体部署单元承载商城、秒杀、后台、好友圈与即时通信全部模块；模块内部自治，模块间经 service 接口进行进程内调用（面向接口，非 HTTP），保持未来拆分的可能。单体消除了网络分区、服务发现与分布式事务问题，让学习焦点保持在 Go 后端本身；模块边界与接口抽象（见 ADR-0003）保留了演进空间。演示前端与文档站不在单体部署单元内：前者以 REST 契约为界独立构建，后者为独立静态站点。
