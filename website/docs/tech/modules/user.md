---
sidebar_position: 2
---

# user — 用户与认证

**定位**：注册（bcrypt 加密）/ 登录（JWT，2h）/ 个人信息 / 地址簿（默认地址唯一）。

**状态**：占位。数据模型 / 接口 / 时序待后续填充（实现：`internal/user/`）。

相关：admin 种子账号 admin/admin123 由迁移种入（见[演示账号](../../user-guide/demo-accounts)）。
