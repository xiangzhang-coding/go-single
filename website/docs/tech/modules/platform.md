---
sidebar_position: 11
---

# platform — 共享基础设施

**定位**：config（viper）/ logger（zap）/ metrics（Prometheus）/ auth（JWT）/ limiter / cors / cron / mq（RabbitMQ）/ cache（Redis）/ ws / file（MinIO 上传）。可替换组件集中于本层，替换成本低（见 BACKLOG）。

**状态**：占位。数据模型 / 接口 / 时序待后续填充（实现：`internal/platform/`）。
