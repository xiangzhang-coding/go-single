---
sidebar_position: 5
---

# order — 订单

**定位**：购物车结算 / 直购下单（单事务：订单 + 订单项 + 库存扣减 + 地址快照 + 券核销 + 清购物车）、`client_request_id` 幂等、雪花订单号、状态机（待支付→已支付→已发货→已完成，含取消与超时取消）、秒杀异步落单与超时回补。

**状态**：占位。数据模型 / 接口 / 时序待后续填充（实现：`internal/order/`）。

领域：见[交易域](../domains/trade)。
