# Go 单体商城

用于学习 Go 后端技术栈的模块化单体项目：在线商城、秒杀、后台管理、好友圈社交与即时通信，以就业向主流选型（Gin/GORM/MySQL/Redis/RabbitMQ）实现。

## Language

### 架构

**模块化单体 (Modular Monolith)**：
以单一部署单元承载多个业务模块，模块间经 service 接口进行进程内调用（面向接口，非 HTTP），保持未来拆分的可能。
_Avoid_: 微服务，单层堆叠的 main.go

**演示前端 (Demo Frontend)**：
对接后端 API 的演示用前端，以"每套主题一个独立 Vite 工程"的形式存在于 web/ 目录，与后端以 REST 接口契约为界，互不耦合。
_Avoid_: 客户端，前台页面

### 商品域

**商品 (Product / SPU)**：
标准化的售卖单元，含标题、详情、类目，与规格无关。SPU 为行业称谓，与"商品"同义。
_Avoid_: 产品，物品

**SKU (SKU)**：
具体可售的库存单元，绑定规格组合、价格与库存数量。
_Avoid_: 变体，规格项

**购物车 (Cart)**：
用户暂存待购条目的集合，条目引用 SKU。
_Avoid_: 购物袋，暂存单

### 交易域

**订单 (Order)**：
用户发起的购买请求，具有明确的生命周期状态（待支付→已支付→已发货→已完成，含取消与超时取消）；以 order_type 区分普通订单与秒杀订单，秒杀订单不使用优惠券。
_Avoid_: 交易，流水

**地址簿 (Address Book)**：
用户维护的当前收货地址集合，下单时从中选择一条固化为地址快照。
_Avoid_: 通讯录，收货地址（指单条时）

**地址快照 (Address Snapshot)**：
下单时固化到订单上的收货地址副本，不受用户后续修改地址的影响。
_Avoid_: 收货地址（指当前地址时）

**秒杀活动 (Flash Sale)**：
限定时间窗口与库存数量的抢购促销，通过预扣库存与异步落单应对高并发。
_Avoid_: 促销，抢购页

**预扣 (Pre-deduct)**：
秒杀抢购时先从 Redis 库存原子扣减、随后异步落单的机制。
_Avoid_: 扣库存（泛指时）

**模拟支付 (Mock Payment)**：
不接入真实渠道，由内部接口模拟支付成功/失败回调以驱动订单状态流转的机制。
_Avoid_: 支付网关，真实支付

**优惠券 (Coupon)**：
可核销的折扣凭证，由后台发布券模板、用户领取、下单时使用；与秒杀互斥。
_Avoid_: 代金券，折扣码

### 社交域

**好友 (Friend)**：
经双方确认的用户关系。
_Avoid_: 粉丝，关注者

**好友申请 (Friend Request)**：
好友关系建立流程中的中间状态（待处理→通过/拒绝）。
_Avoid_: 添加请求

**好友圈 (Friends Feed)**：
仅好友可见的购买分享时间线。
_Avoid_: 朋友圈，动态墙

**动态 (Post)**：
好友圈中的一条分享，引用已购 SKU 与可选文案。
_Avoid_: 帖子，分享记录

### 通信域

**消息 (Message)**：
即时通信中单条用户间内容，落库并支持离线拉取。
_Avoid_: 聊天记录条目，私信内容

**图片消息 (Image Message)**：
含图片内容的消息，图片经 MinIO 存储，消息引用其 URL。
_Avoid_: 附件

**文件消息 (File Message)**：
含图片以外文件内容的消息，文件经 MinIO 存储，消息引用其 URL。
_Avoid_: 附件

### 运营域

**对账 (Reconciliation)**：
定时比对 Redis 库存、MySQL 库存与有效订单数以发现并补偿差异的机制。
_Avoid_: 盘点（指线下库存时）
