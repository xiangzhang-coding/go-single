# Cloudflare Pages 响应头（T28 部署收尾）：与本地 Nginx 安全头对齐（DESIGN「安全设计 → HTTPS 与安全头」）。
# Cloudflare Pages 默认强制 HTTPS；媒体由 API 鉴权读取为 blob，不直连 MinIO。
/*
  X-Content-Type-Options: nosniff
  X-Frame-Options: DENY
  Referrer-Policy: no-referrer
  Strict-Transport-Security: max-age=31536000; includeSubDomains
  Content-Security-Policy: default-src 'self'; script-src 'self'; style-src 'self' 'unsafe-inline'; img-src 'self' data: blob: https:; font-src 'self' data:; connect-src 'self' wss: https:
